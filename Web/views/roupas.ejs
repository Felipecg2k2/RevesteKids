<!DOCTYPE html>
<html lang="pt-br">
<head>
    <meta charset="UTF-8">
    <title>Minhas Roupas para Troca</title>
    <style>
        /* CSS MÍNIMO: Apenas o ESSENCIAL */
        
        /* Estilos para o Modal (Overlay de Cadastro/Edição) */
        #modal-cadastro {
            display: none; /* Oculta por padrão */
            position: fixed; 
            z-index: 100; 
            left: 0;
            top: 0;
            width: 100%;
            height: 100%;
            background-color: rgba(0,0,0,0.5); 
            overflow-y: auto; 
        }
        .modal-content {
            background-color: #fefefe;
            margin: 5% auto; 
            padding: 20px;
            border: 1px solid #888;
            width: 80%; 
            max-width: 600px;
            border-radius: 8px;
            position: relative;
        }
        .close {
            color: #aaa;
            float: right;
            font-size: 28px;
            font-weight: bold;
            cursor: pointer;
        }
        
        /* Estilos para o Grid de Itens */
        .itens-grid {
            display: grid;
            grid-template-columns: repeat(auto-fill, minmax(200px, 1fr));
            gap: 20px;
            margin-top: 20px;
        }
        .item-card {
            border: 1px solid #ddd;
            padding: 10px;
            border-radius: 8px;
            box-shadow: 1px 1px 3px rgba(0,0,0,0.05);
            text-align: center;
        }

        /* Estilos para os Cards de Métricas (Contadores) */
        .metric-card { 
            padding: 15px;
            border-radius: 8px;
            text-align: center;
            display: block; 
        }

        /* Estilos para o Status de Posse */
        .status {
            font-size: 0.9em;
            margin-top: 5px;
        }
        .status-ativo {
            color: green;
        }
        .status-troca {
            color: orange;
        }
        
    </style>
</head>
<body>
    
    <div 
        id="item-data-container" 
        style="display: none;"
        data-item='<%- typeof itemParaEditar !== 'undefined' && itemParaEditar ? JSON.stringify(itemParaEditar) : 'null' %>'
    ></div>

    <h1>Gerenciamento de Peças (Minhas Roupas)</h1>
    
    <div style="text-align: center; margin-bottom: 20px;">
        <button id="btn-abrir-modal" style="
            background-color: #9370DB; color: white; padding: 10px 20px; border: none; border-radius: 4px; cursor: pointer; font-size: 16px;
        ">
            + Adicionar Roupa
        </button>
    </div>
    
    <div class="metrics-bar" style="display: flex; justify-content: space-around; margin-bottom: 30px; border-bottom: 1px solid #eee; padding-bottom: 15px;">
        
        <a href="/roupas?status=Ativo" style="text-decoration: none; color: inherit;">
            <div class="metric-card" style="background-color: #e0f7fa;">
                <strong><%= typeof totalCadastradas !== 'undefined' ? totalCadastradas : 0 %></strong>
                <p>Roupas Cadastradas</p>
            </div>
        </a>
        
        <a href="/roupas?status=EmTroca" style="text-decoration: none; color: inherit;">
            <div class="metric-card" style="background-color: #fffde7;">
                <strong><%= typeof emTroca !== 'undefined' ? emTroca : 0 %></strong>
                <p>Em Processo de Troca</p>
            </div>
        </a>

        <a href="/trocas/historico" style="text-decoration: none; color: inherit;">
            <div class="metric-card" style="background-color: #e8f5e9;">
                <strong><%= typeof trocasRealizadas !== 'undefined' ? trocasRealizadas : 0 %></strong>
                <p>Trocas Realizadas</p>
            </div>
        </a>

    </div>

    <div class="itens-grid">
        <% if (itens && itens.length > 0) { %>
            <% itens.forEach(function(item) { %>
    <div class="item-card">
        <div style="height: 150px; background-color: #f0f0f0; margin-bottom: 10px; display: flex; justify-content: center; align-items: center;">
            [Imagem da Peça]
        </div>
        
        <h3><%= item.peca %></h3>
        
        <p>Tam: <%= item.tamanho %></p>
        
        <p style="font-size: 0.9em; color: #666; margin-top: -10px;">
            <%= item.categoriaPeca %>
        </p>
        
        <p class="status <%= item.statusPosse === 'Ativo' ? 'status-ativo' : 'status-troca' %>">
            <%= item.statusPosse === 'Ativo' ? 'Disponível' : 'Em Processo de Troca' %>
        </p> 
        
        <div style="display: flex; justify-content: flex-end; gap: 10px; margin-top: 10px;">
            <a href="/roupas/editar/<%= item.id %>" title="Editar" style="text-decoration: none;">✏️</a> 
            <a href="/roupas/excluir/<%= item.id %>" onclick="return confirm('Excluir <%= item.peca %>?');" title="Excluir" style="color: red; text-decoration: none;">🗑️</a>
        </div>
    </div>
<% }); %>
        <% } else { %>
            <p>Você ainda não cadastrou nenhuma peça. Clique no botão "Adicionar Roupa" para começar.</p>
        <% } %>
    </div>

    <div id="modal-cadastro">
        <div class="modal-content">
            <span id="btn-fechar-modal" class="close">&times;</span>

            <h1 id="modal-titulo">Cadastrar Nova Peça para Troca</h1>
            
            <form id="form-roupa" method="POST" action="/roupas/salvar"> 
    <input type="hidden" id="item-id" name="id" value=""> 

    <label for="peca">Nome da Peça (Ex: Vestido Floral, Calça Jeans):</label>
    <input type="text" id="peca" name="peca" required><br><br>

    <label for="categoriaPeca">Tipo da Peça (Categoria):</label>
    <select id="categoriaPeca" name="categoriaPeca" required>
        <option value="">Selecione a Categoria</option>
        <option value="Blusa">Blusa</option>
        <option value="Camisa">Camisa</option>
        <option value="Calça">Calça</option>
        <option value="Vestido">Vestido</option>
        <option value="Saia">Saia</option>
        <option value="Macacão">Macacão</option>
        <option value="Shorts">Shorts</option>
        </select><br><br>

    <label for="tipo">Gênero:</label>
    <select id="tipo" name="tipo" required>
        <option value="Feminino">Feminino</option>
        <option value="Masculino">Masculino</option>
        <option value="Unissex">Unissex</option>
    </select><br><br>
    
    <label for="tamanho">Tamanho (Ex: 3-4, M, G):</label>
    <input type="text" id="tamanho" name="tamanho" required><br><br>
    
    <label for="cor">Cor:</label>
    <input type="text" id="cor" name="cor"><br><br>

    <label for="tecido">Tecido:</label>
    <input type="text" id="tecido" name="tecido"><br><br>

    <label for="estacao">Estação (Ex: Verão, Inverno):</label>
    <input type="text" id="estacao" name="estacao"><br><br>
    
    <label for="condicao">Condição (Ex: Novo, Com pouco uso):</label>
    <input type="text" id="condicao" name="condicao" required><br><br>

    <label for="descricao">Descrição Detalhada:</label>
    <textarea id="descricao" name="descricao"></textarea><br><br>

    <button type="submit" id="btn-submit">Cadastrar Roupa</button>
</form>
        </div>
    </div>

    <p style="clear: both;"><a href="/dashboard">Voltar para o Dashboard</a></p>

    <script>
        // O JS agora lê os dados do atributo 'data-item'
        const dataContainer = document.getElementById('item-data-container');
        let itemData = dataContainer.getAttribute('data-item');

        // Garante que a variável global seja definida (para compatibilidade com roupas.js)
        if (itemData && itemData !== 'null') {
            // Se JSON.parse falhar (por causa de caracteres escapados), tentamos corrigir.
            try {
                window.itemParaEditar = JSON.parse(itemData);
            } catch (e) {
                // Tenta limpar entidades HTML que EJS pode ter introduzido.
                itemData = itemData.replace(/&#34;/g, '"').replace(/&#39;/g, "'");
                try {
                    window.itemParaEditar = JSON.parse(itemData);
                } catch (e) {
                    console.error("Erro final ao parsear itemParaEditar:", e);
                    window.itemParaEditar = null; 
                }
            }
        } else {
            window.itemParaEditar = null;
        }
    </script>
    
    <script src="/js/roupas.js"></script>
</body>
</html>